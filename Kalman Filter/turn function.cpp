#include <iostream>

using namespace std;

void move(float L, float R){
	int left = L;
	int right = R;
}

void turn(float target, float current){
	if(L.rpm < R.rpm){
		R.rpm = L.rpm;
	}
	else if(L.rpm > R.rpm){
		L.rpm = R.rpm;
	}
}

int main(){
	float current = 0.0, target = 200.0;
	return 0;
} 
